version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.12

    working_directory: /go/src/github.com/nestorsalceda/cloud-native-visibility-hub-backend
    steps:
      - checkout

      - run: go get -v -d ./...
      - run: go test -v ./...
